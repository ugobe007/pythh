# Legacy frontend (not deployed to pythh.ai)

This `src/` tree is the **original hot-honey React app** (`react-router-dom`, `/signal-matches`, older admin surfaces).

**Production pythh.ai** is built from **`site/`** (see repo-root `vite.config.ts` and `PYTHH_AI_CURSOR_COPILOT_HANDOFF.md`).

Do not add new product features here unless explicitly migrating or maintaining legacy admin tools.
